<form method="post" action="{{ path('app_camping_delete', {'idCamping': camping.idCamping}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ camping.idCamping) }}">
    <button class="btn">Delete</button>
</form>
