{% extends 'base.html.twig' %}

{% block title %}Participation{% endblock %}

{% block body %}
    <h1>Participation</h1>

    <table class="table">
        <tbody>
            
            <tr>
                <th>Refp</th>
                <td>{{ participation.refp }}</td>
            </tr>
           
            <tr>
                <th>Nombre</th>
                <td>{{ participation.nombre }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ participation.montant }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ participation.etat }}</td>
            </tr>
            <tr>
                <th>DateParti</th>
                <td>{{ participation.dateParti ? participation.dateParti|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ participation.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_participation_index') }}">back to list</a>

    <a href="{{ path('app_participation_edit', {'idParti': participation.idParti}) }}">edit</a>

    {{ include('participation/_delete_form.html.twig') }}
{% endblock %}
